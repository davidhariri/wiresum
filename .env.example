# Wiresum Configuration
# Copy to .env and fill in your values

# =============================================================================
# REQUIRED
# =============================================================================

# Groq API key for Llama (https://console.groq.com)
GROQ_API_KEY=gsk_...

# Feedbin credentials (https://feedbin.com)
FEEDBIN_EMAIL=your-email@example.com
FEEDBIN_PASSWORD=your-feedbin-password

# =============================================================================
# OPTIONAL
# =============================================================================

# Firecrawl API key for better article content extraction (https://firecrawl.dev)
# Without this, falls back to basic HTML parsing which works but is less reliable
# FIRECRAWL_API_KEY=fc-...

# SQLite database path (default: /data/wiresum.db in Docker)
# WIRESUM_DB_PATH=/data/wiresum.db

# Server URL for CLI to connect to (default: http://localhost:8000)
# WIRESUM_SERVER_URL=http://localhost:8000
